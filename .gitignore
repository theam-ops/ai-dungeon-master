__pycache__/
*.pyc
campaign.db
campaign.db-wal
campaign.db-shm
.session_secret
saves/
.venv/
.keys.json
media/
.pytest_cache/

# Scratch checkouts that background agents work in. Each is a full copy of the
# repo with its own .git, so committing one would duplicate the whole project.
.claude/worktrees/

# the tunnel binary, fetched on demand by `launch.py --share`
tools/bin/
