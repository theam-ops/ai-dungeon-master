# Everything the game needs, plus the test runner.
#
#     pip install -r requirements-dev.txt
#     python -m pytest
#
# The suite stubs every backend, so it needs no API key and spends nothing.

-r requirements.txt

pytest>=8
